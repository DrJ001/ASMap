# Internal fastMap functions

These are internal `fastMap` functions that are not intended to be
directly called by the user

## Usage

``` r
# Default S3 method
mstmap(object, ...)
```

## Author

Julian Taylor
